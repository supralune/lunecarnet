---
interface Props { title?: string; compact?: boolean; enabled?: boolean }
const { title = "Template editing guide", compact = false, enabled = true } = Astro.props;
---

{enabled && (
  <aside class:list={["template-note", { compact }]} aria-label={title}>
    <strong>{title}</strong>
    <div><slot /></div>
  </aside>
)}
