---
import { BlogAboutPage, getPublishedPosts } from "@lunecarnet/astro";
import { blogTemplate } from "@/config/site";

const posts = await getPublishedPosts();
---

<BlogAboutPage posts={posts} {...blogTemplate} />
