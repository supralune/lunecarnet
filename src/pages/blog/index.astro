---
import { BlogHomePage, getPublishedPosts } from "@lunecarnet/astro";
import { blogTemplate } from "@/config/site";

const posts = await getPublishedPosts();
---

<BlogHomePage posts={posts} {...blogTemplate} />
