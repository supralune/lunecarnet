---
import { BlogArchivePage, getPublishedPosts } from "@lunecarnet/astro";
import { blogTemplate } from "@/config/site";

const posts = await getPublishedPosts();
---

<BlogArchivePage posts={posts} {...blogTemplate} />
